---
phase: 02-comprehensive-logging
plan: 04
type: execute
wave: 2
depends_on: [02-01, 02-02, 02-03]
files_modified: [api/index.py, lib/storage.py]
autonomous: true

must_haves:
  truths:
    - "Sync function logs full cycle with correlation ID"
    - "Each sync invocation has unique correlation ID"
    - "Logs persisted to Redis for debugging"
  artifacts:
    - path: "api/index.py"
      contains: ["get_logger", "set_correlation_id", "logger.info"]
      min_lines: 650
    - path: "lib/storage.py"
      contains: ["append_log", "get_logs"]
  key_links:
    - from: "api/index.py"
      to: "lib.logger.get_logger"
      via: "import"
    - from: "api/index.py"
      to: "lib.logger.set_correlation_id"
      via: "sync() function"
    - from: "lib/storage.py"
      to: "Redis list operations"
      via: "LPUSH/LTRIM pattern"
---

<objective>
Add structured logging to the main sync function and implement Redis log persistence.

Purpose: The sync function is the entry point - we need full visibility into each sync cycle including correlation IDs for tracing. We also need to persist logs to Redis so they're available for debugging after Vercel's ephemeral logs expire.
Output: api/index.py with comprehensive sync logging, storage.py with log persistence
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Requirements:**
- LOG-02: Redis log handler persists last 500 entries
- LOG-03: Structured logging with correlation IDs (sync function)

**Depends on:** 02-01, 02-02, 02-03 (logger and module logging must exist)

**Current files:**
@api/index.py - Main sync() function, needs correlation ID and comprehensive logging
@lib/storage.py - Needs log persistence functions

**Key integration points:**
1. sync() function sets correlation ID at start
2. sync() logs each phase of the sync cycle
3. Logs persisted to Redis with 500 entry limit
4. Dashboard can retrieve recent logs for display
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Redis log persistence to storage.py</name>
  <files>lib/storage.py</files>
  <action>
Add Redis log persistence functions to lib/storage.py:

1. **Log constants**: 
   - `LOG_KEY = 'logs'`
   - `MAX_LOG_ENTRIES = 500`

2. **append_log() function**:
   ```python
   def append_log(log_entry: dict) -> bool:
       """Append a log entry to Redis (keeps last 500)."""
       # Use Redis list: LPUSH to add, LTRIM to keep last 500
       # log_entry should have: timestamp, level, name, message, correlation_id
   ```

3. **get_logs() function**:
   ```python  
   def get_logs(count: int = 100) -> list:
       """Get recent log entries from Redis."""
       # Use LRANGE to get last N entries
       # Return list of log dicts
   ```

4. **Integration with existing logger** (if RedisLogHandler from 02-01 exists):
   - Ensure RedisLogHandler uses these functions
   - Or add flush_logs() if buffering is implemented

5. **Add logging** for log operations themselves (DEBUG level)

Add these functions after the existing batch_update function.
  </action>
  <verify>python -c "from lib.storage import append_log, get_logs; print('Log persistence OK')"</verify>
  <done>append_log() and get_logs() functions exist and are importable</done>
</task>

<task type="auto">
  <name>Task 2: Add comprehensive logging to api/index.py sync function</name>
  <files>api/index.py</files>
  <action>
Add structured logging with correlation IDs to api/index.py:

1. **Imports**: Add `from lib.logger import get_logger, set_correlation_id, clear_correlation_id, get_correlation_id`

2. **Module-level logger**: `logger = get_logger('api')` after imports

3. **sync() function logging**:
   - At function entry: Generate/set correlation ID with `set_correlation_id()`
   - INFO: "=== Sync cycle started ===" with correlation_id
   - DEBUG: "Environment: Vercel={is_vercel}, Region={region}"
   
   Each phase of sync:
   - INFO: "Loading state from storage"
   - DEBUG: "Current state: {state_summary}"
   - INFO: "Checking flood wait" (log if rate limited)
   - INFO: "Checking Spotify tokens"
   - INFO: "Token refresh required" (if applicable)
   - INFO: "Token refreshed successfully" (after refresh)
   - INFO: "Fetching current track from Spotify"
   - DEBUG: "Track info: {track}"
   - INFO: "Checking update criteria"
   - DEBUG: "should_update: {result}, last_key: {key}, elapsed: {seconds}s"
   - INFO: "Update needed: {reason}" (or "No update needed")
   - INFO: "Updating Telegram last name to: {name}"
   - INFO: "Telegram update successful" (or "Rate limited" or "Failed")
   - INFO: "Saving state to storage"
   
   At function exit:
   - INFO: "=== Sync cycle completed: {action} ===" (action = updated/skipped/rate_limited/error)
   - Always clear correlation ID with `clear_correlation_id()`

4. **Other endpoints**:
   - status(): DEBUG: "Status requested"
   - spotify_auth(): INFO: "Spotify OAuth initiated"
   - spotify_callback(): INFO: "Spotify OAuth callback received"
   - init(): INFO: "Initialization requested"

5. **Exception handling**:
   - ERROR: "Sync failed: {error}" with full exception info
   - ERROR: "Traceback: {traceback}"
   - Include correlation_id in all error logs

6. **Integration**:
   - Ensure correlation ID flows to spotify.py, telegram.py, storage.py via logger context
   - Test that logs appear in JSON format

Maintain all existing functionality. Only add logging calls.
  </action>
  <verify>python -c "from api.index import app; print('api/index.py imports OK')"</verify>
  <done>api/index.py has logging, imports work, all endpoints still functional</done>
</task>

<task type="auto">
  <name>Task 3: Wire Redis log handler in logger.py</name>
  <files>lib/logger.py</files>
  <action>
Ensure RedisLogHandler in lib/logger.py properly uses storage.append_log():

1. **Update RedisLogHandler**:
   - Import `append_log` from storage (if not already done)
   - In emit() method, format log record as dict with:
     - timestamp: ISO8601 format
     - level: log level name
     - name: logger name
     - message: formatted message
     - correlation_id: current correlation ID
     - module: module name
     - function: function name
     - line: line number
   - Call `append_log(log_dict)`
   - Handle any errors gracefully (don't let logging failures break app)

2. **Buffering** (optional but recommended):
   - Buffer up to 10 logs in memory
   - Flush on buffer full or after 5 seconds
   - Use threading.Lock for thread safety

3. **Configuration**:
   - RedisLogHandler added to logger in get_logger() when Redis available
   - Log level should be INFO or lower to capture relevant logs

Update logger.py if RedisLogHandler needs adjustments to work with storage.py functions.
  </action>
  <verify>python -c "from lib.logger import get_logger; l = get_logger('test'); l.info('test'); print('Redis handler OK')"</verify>
  <done>RedisLogHandler emits logs to storage.append_log(), no errors</done>
</task>

</tasks>

<verification>
After completion:
1. `python -c "from api.index import app"` succeeds
2. sync() function generates correlation IDs
3. Logs flow through to Redis (can verify with get_logs())
4. JSON format preserved across all log entries
5. Each sync cycle has unique correlation ID
</verification>

<success_criteria>
- [ ] api/index.py uses get_logger and correlation ID functions
- [ ] storage.py has append_log() and get_logs() functions
- [ ] RedisLogHandler properly wired to storage.append_log()
- [ ] sync() logs full cycle with correlation ID
- [ ] Logs persist to Redis (last 500 entries)
- [ ] No import errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-comprehensive-logging/02-04-SUMMARY.md`
</output>
