---
phase: 01-dashboard-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/index.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Dashboard no longer shows 'Now Playing' section"
    - "Timestamps display as absolute datetime in GMT+3 format"
    - "Dashboard shows 'Syncing...' indicator during sync"
  artifacts:
    - path: "api/index.py"
      provides: "DASHBOARD_HTML with removed Now Playing section"
      contains: "Now Playing" (should NOT contain)
    - path: "api/index.py"
      provides: "Status API with absolute timestamps"
      contains: "last_sync_formatted"
    - path: "api/index.py"
      provides: "JavaScript with syncing indicator"
      contains: "syncing"
---

<objective>
Fix dashboard UI: remove Now Playing section, add GMT+3 timestamp formatting, add syncing indicator.

Purpose: Improve user-facing dashboard per requirements UI-01 and context decisions. Remove irrelevant "Now Playing" section, show timestamps in absolute format with GMT+3 timezone, and show "Syncing..." during active sync.
</objective>

<context>
@api/index.py
# Review DASHBOARD_HTML structure around line 196 (Now Playing section)
# Review status() function around line 229 for timestamp fields
# Review renderStatus() JavaScript function for display logic
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Now Playing section from dashboard</name>
  <files>api/index.py</files>
  <action>
In DASHBOARD_HTML, find and remove the "Now Playing" card section. Around line 196, there is:
```
'<div class="card"><div class="card-header"><span class="card-title">Now Playing</span></div>' + trackHtml + '</div>' +
```

Remove this entire line (including the trailing '+'). The variable trackHtml can be removed since it's no longer used. Also remove the track variable from the status() JSON response (line 254).
  </action>
  <verify>grep -n "Now Playing" api/index.py returns no matches</verify>
  <done>Dashboard HTML no longer contains "Now Playing" card section</done>
</task>

<task type="auto">
  <name>Task 2: Add GMT+3 absolute timestamp formatting to status API</name>
  <files>api/index.py</files>
  <action>
In the status() function (around line 229), add formatted timestamp fields to the JSON response:
1. Import datetime at top of file: `from datetime import datetime`
2. Add a helper function `format_gmt3(ts)` that returns formatted string like "Feb 15, 2026 3:45 PM GMT+3" or "-" if ts is 0/None
3. Add to sync object in response:
   - `last_sync_formatted`: format_gmt3(last_sync)
   - `last_update_formatted`: format_gmt3(last_update)

The function should handle zero/None gracefully returning placeholder like "Never".
  </action>
  <verify>Call /api/status and verify response contains last_sync_formatted and last_update_formatted in correct format</verify>
  <done>Status API returns GMT+3 formatted timestamps as "Feb 15, 2026 3:45 PM GMT+3"</done>
</task>

<task type="auto">
  <name>Task 3: Update dashboard JavaScript for GMT+3 timestamps and syncing indicator</name>
  <files>api/index.py</files>
  <action>
In DASHBOARD_HTML JavaScript section:

1. Update renderStatus() function to:
   - Display last_sync_formatted instead of formatTimeAgo for "Last Sync" (line 199)
   - Display last_update_formatted instead of formatTimeAgo for "Last Update" (line 200)
   - Add "Syncing..." indicator that shows during active sync

2. Add syncing state tracking:
   - Call /api/sync endpoint to trigger sync
   - Show "Syncing..." text in the Sync Status card while request is in flight
   - After sync completes, refresh status to show updated timestamps
   - Add a "Sync Now" button or link to trigger manual sync

3. Update stat-label to indicate format: "Last Sync" and "Last Update" labels remain, but display absolute time
  </action>
  <verify>Dashboard shows absolute timestamps and has working sync trigger with "Syncing..." indicator</verify>
  <done>Dashboard displays timestamps in "Feb 15, 2026 3:45 PM GMT+3" format with syncing indicator</done>
</task>

</tasks>

<verification>
- [ ] /api/status returns last_sync_formatted and last_update_formatted fields
- [ ] DASHBOARD_HTML contains no "Now Playing" text
- [ ] Dashboard renders with absolute GMT+3 timestamps
- [ ] Dashboard shows "Syncing..." during sync operations
</verification>

<success_criteria>
Dashboard UI complete with:
- No "Now Playing" section visible
- Timestamps shown as "Feb 15, 2026 3:45 PM GMT+3" format
- Syncing indicator appears during sync
</success_criteria>

<output>
After completion, create `.planning/phases/01-dashboard-bug-fixes/01-01-SUMMARY.md`
</output>
