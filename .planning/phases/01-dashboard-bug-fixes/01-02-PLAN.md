---
phase: 01-dashboard-bug-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - api/index.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "last_sync timestamp updates on EVERY sync invocation"
    - "last_update timestamp updates when track/name actually changes"
    - "Sync state is always persisted to storage"
  artifacts:
    - path: "api/index.py"
      provides: "sync function that always saves last_sync"
      contains: "last_sync"
---

<objective>
Fix backend sync state bugs: ensure last_sync always updates and last_update is correct.

Purpose: Fix BUG-01 (last_sync not saved when no update needed) and BUG-02 (last_update timestamp issues). The sync function must ALWAYS save last_sync on every invocation, and save last_update only when track/name actually changes.
</objective>

<context>
@api/index.py
# Review sync() function, especially lines 377-532
# Focus on: lines 475-497 (skip paths), lines 504-513 (success path)
# Also review lines 480-484 and 492-496 where needs_save gating causes bug
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix last_sync always saving on every sync invocation (BUG-01)</name>
  <files>api/index.py</files>
  <action>
In the sync() function, fix the bug where last_sync is only saved when needs_save is True:

1. Around lines 475-485 (should_update returns False - no update needed):
   - Move `state['last_sync'] = time.time()` OUTSIDE the `if needs_save:` block
   - Always set last_sync and always call storage.batch_update()
   - The current_track should still be saved to show what's currently playing

2. Around lines 488-497 (name unchanged - desired_name == current_last_name):
   - Same fix: move last_sync update outside the needs_save conditional
   - Always persist the state with updated last_sync timestamp

The key change: `last_sync` should ALWAYS be updated and saved, regardless of whether an actual update was performed. This is the core fix for BUG-01.

Before (buggy):
```python
if needs_save:
    state['last_sync'] = time.time()
    updates['sync_state'] = state
    ...
```

After (fixed):
```python
state['last_sync'] = time.time()
updates['sync_state'] = state
# Always save current track
updates['current_track'] = track.to_dict() if track else {'is_playing': False}
storage.batch_update(**updates)
```

Make sure these early return paths ALWAYS call storage.batch_update() with the state containing last_sync.
  </action>
  <verify>Run sync twice - verify last_sync updates both times even when no update occurs</verify>
  <done>last_sync timestamp is persisted to storage on every sync invocation</done>
</task>

<task type="auto">
  <name>Task 2: Verify and fix last_update timestamp logic (BUG-02)</name>
  <files>api/index.py</files>
  <action>
Review and fix last_update timestamp behavior:

1. In the success path (lines 504-513): last_update is correctly set when telegram update succeeds - this is correct.

2. In the skip paths (lines 475-485 and 488-497): last_update should NOT be updated because nothing actually changed. This is correct behavior - last_update should reflect when the name LAST CHANGED, not when we last checked.

3. Ensure that when an update IS performed (line 507), last_update is set to time.time() - verify this is working.

The bug might be: ensure last_update is NOT accidentally updated in skip paths. Verify:
- should_update returns False: last_update unchanged (correct)
- name unchanged: last_update unchanged (correct)  
- update succeeds: last_update = time.time() (correct)

If last_update is being incorrectly set in skip paths, fix by ensuring those paths do NOT set last_update.
  </action>
  <verify>Perform sync that changes name - verify last_update updates. Perform sync that skips - verify last_update unchanged.</verify>
  <done>last_update timestamp correctly reflects when track/name actually changed</done>
</task>

<task type="auto">
  <name>Task 3: Ensure current_track is always saved with sync state</name>
  <files>api/index.py</files>
  <action>
Ensure that the current_track is always saved to storage on every sync invocation, not just when needs_save is True:

In all code paths (success, skip, rate limited), after determining the track, save it:
- updates['current_track'] = track.to_dict() if track else {'is_playing': False}

This ensures the dashboard always shows the current track info regardless of whether an update was needed.
  </action>
  <verify>Run sync and verify current_track is saved to storage</verify>
  <done>current_track is persisted on every sync invocation</done>
</task>

</tasks>

<verification>
- [ ] last_sync is saved on every sync call (even when skipped)
- [ ] last_update is only saved when name actually changes
- [ ] current_track is always persisted
- [ ] Storage is updated via batch_update in all code paths
</verification>

<success_criteria>
Backend sync fixes complete:
- BUG-01 fixed: last_sync always updates on every invocation
- BUG-02 fixed: last_update correctly reflects last actual change
- Current track always saved to storage
</success_criteria>

<output>
After completion, create `.planning/phases/01-dashboard-bug-fixes/01-02-SUMMARY.md`
</output>
